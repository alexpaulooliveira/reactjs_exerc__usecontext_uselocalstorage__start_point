import AppContext, { IAppContext, defaultAppContext } from './AppContext';
import ContextSwitcher from './ContextSwitcher';

const App = () => {  
  return (
    <div>por fazer...</div>
  );
};

export default App